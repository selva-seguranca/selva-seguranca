# Vercel only
# Use ONLY these variables for database access in Vercel.
# Do not create DB_HOST / DB_PORT / DB_DATABASE / DB_USERNAME / DB_PASSWORD in Vercel.

DATABASE_URL=postgresql://postgres.<project_ref>:YOUR_DB_PASSWORD@aws-<region>.pooler.supabase.com:6543/postgres
DB_EMULATE_PREPARES=true

APP_ENV=production
APP_URL=https://your-project.vercel.app
APP_DEBUG=false

CHECKLIST_STORAGE_DRIVER=supabase
CHECKLIST_MAX_FILE_SIZE_MB=4

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_STORAGE_BUCKET=checklists
SUPABASE_STORAGE_DIRECTORY=checklists
SUPABASE_STORAGE_KEY=your-supabase-service-role-key
SUPABASE_STORAGE_PUBLIC_URL=

JWT_SECRET=replace-with-a-long-random-secret

# Notes
# 1. Copy DATABASE_URL exactly from Supabase > Connect > Transaction Pooler.
# 2. The pooler username is usually postgres.<project_ref>, not just postgres.
# 3. Do not use db.<project-ref>.supabase.co:6543 on Vercel.
